# Commandes à lancer pour setup l'environnement des tests unitaire.

- ```npm init -y``` [génère un package.json]
- ```npm install --save-dev jest```
- ```npm install --save d3```
- ```npm install --save-dev jest-environment-jsdom```

- ```npm run test```